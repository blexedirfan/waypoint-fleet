services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: waypoint_fleet
      POSTGRES_USER: waypoint
      POSTGRES_PASSWORD: waypoint
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
